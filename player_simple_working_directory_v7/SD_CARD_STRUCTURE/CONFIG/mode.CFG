# Tactile Communicator mode configuration (SD pack)
# Priority mode: HUMAN_FIRST or GENERATED_FIRST
PRIORITY=HUMAN_FIRST
# Strict playlists: 1 (enabled) or 0 (disabled)
# Current firmware uses directory-based discovery; set to 0.
STRICT_PLAYLISTS=0
